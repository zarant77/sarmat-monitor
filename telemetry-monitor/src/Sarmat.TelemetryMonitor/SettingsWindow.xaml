<Window x:Class="Sarmat.TelemetryMonitor.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Telemetry Monitor Settings" Width="560" Height="245"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="145" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Aggregator URL" VerticalAlignment="Center" />
        <TextBox x:Name="UrlBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Padding="6" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Secret" VerticalAlignment="Center" />
        <TextBox x:Name="SecretBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Padding="6" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Reconnect interval" VerticalAlignment="Center" />
        <DockPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <TextBlock DockPanel.Dock="Right" Text="seconds" VerticalAlignment="Center" Margin="8,0,0,0" />
            <TextBox x:Name="ReconnectBox" Padding="6" />
        </DockPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="3" Grid.ColumnSpan="2" Foreground="#B91C1C"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel" Width="88" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Save" Width="88" IsDefault="True" Click="SaveClick" />
        </StackPanel>
    </Grid>
</Window>
